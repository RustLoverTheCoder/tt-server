[package]
name = "tt-server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[workspace]
members = [".", "api", "core", "migration", "entity", "config"]

[dependencies]
api = { path = "api" }
tracing = "0.1.37"